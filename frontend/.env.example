# API Base URL for the proxy backend
# Example for local development: http://localhost:3000
# Example for production: https://your-proxy-domain.com
VITE_API_BASE_URL=http://localhost:3000

# Base path for GitHub Pages deployment (computed from repo name)
# Normally set automatically based on repository name
# VITE_BASE_PATH=/SAPS-Firearm-Status-Enquiry-PWA/
