# Proxy backend environment variables

# Server port (default: 3000)
PORT=3000

# Node environment (development or production)
# In development, logs are printed to console
# In production, only errors are logged
NODE_ENV=development
